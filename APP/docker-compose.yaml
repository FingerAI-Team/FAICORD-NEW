services:
  app:
    container_name: faicord
    image: faicord:latest
    build:
      context: .
      dockerfile: Dockerfile
    runtime: nvidia
    deploy:
      resources:
        reservations:
          devices:
            - driver: nvidia
              count: all
              capabilities: [gpu]
    volumes:
      - .:/faicord 
    ports:
      - "8081:8081"
    stdin_open: true
    tty: true
    command: bash
    networks:
      - faicord-net
networks:
  faicord-net:
    external: true